<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="XUITheme.Phone">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="StatusView.Custom">
        <item name="sv_complete">@layout/view_status_complete</item>
        <item name="sv_error">@layout/view_status_error</item>
        <item name="sv_loading">@layout/view_status_loading</item>
        <item name="sv_custom">@layout/view_status_custom</item>
    </style>

    <style name="bnve">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginBottom">10dp</item>
    </style>
    <style name="bnve_white_bg" parent="bnve">
        <item name="android:background">@android:color/white</item>
    </style>

    <!-- FabMenu -->
    <style name="FabMenu">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="FabMenu.Buttons">
        <item name="fab_size">mini</item>
        <item name="fab_colorNormal">#3E99D2</item>
        <item name="fab_colorPressed">#2488C6</item>
        <item name="fab_colorRipple">#AF2488C6</item>
    </style>

</resources>
